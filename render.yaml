services:
  - type: worker
    name: perplexity-discord-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python perplexity-bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: PERPLEXITY_API_KEY
        sync: false
